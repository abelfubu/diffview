---
"critique": minor
---

Switch to single-file view with sidebar-focused-by-default

- Sidebar is now focused by default (not the diff pane)
- Diff pane shows only the currently selected file (not a continuous scrollable document)
- j/k in sidebar navigates between files
- j/k in diff pane scrolls within the current file
- Removed scroll-tracking interval and fileRefs (no longer needed)
